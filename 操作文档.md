# 金属厂数据管理系统 — 操作手册

> 版本: v2.4 (短信验证码登录 + 新增未过保物料模块)
> 更新日期: 2026-08-03
> 适用环境: 内网部署 http://服务器地址:端口

---

## 目录

1. [系统概述](#1-系统概述)
2. [快速入门](#2-快速入门)
3. [权限体系](#3-权限体系)
4. [送货记录](#4-送货记录)
5. [原始记录](#5-原始记录)
6. [未过保物料](#6-未过保物料)
7. [送货超比统计](#7-送货超比统计)
8. [结算机台数](#8-结算机台数)
9. [机型明细](#9-机型明细)
10. [开机数量](#10-开机数量)
11. [物料表](#11-物料表)
12. [156项](#12-156项)
13. [公司管理](#13-公司管理) 🔐
14. [操作日志](#14-操作日志) 🔐
15. [用户管理](#15-用户管理) 🔐
16. [Excel导入导出](#16-excel导入导出)
17. [OCR图片识别](#17-ocr图片识别)
18. [定时任务](#18-定时任务)
19. [常见问题](#19-常见问题)

> 🔐 = 仅管理员可访问

---

## 1. 系统概述

金属厂数据管理系统是一套轻量化Web数据管理平台，用于替代传统Excel维护模式，实现数据结构化存储、可视化管理。

### 系统架构

```
浏览器 (Vue 3 + Element Plus)
    ↕ REST API (JWT认证)
后端服务 (Spring Boot 3.3)
    ↕ MyBatis
MySQL 8.4 数据库
```

### 业务模块一览

| 序号 | 模块名称 | 数据量 | 说明 |
|------|----------|--------|------|
| 1 | 送货记录 | 57,000+ | 物料送货全量信息，核心录入表 |
| 2 | 原始记录 | 65,000+ | 金属厂上机/下机原始台账 |
| 3 | 未过保物料 | - | 未过保物料台账，关联维修记录，派生字段自动计算 |
| 4 | 送货超比统计 | 370+ | 月度送货超比数据统计 |
| 5 | 结算机台数 | 160+ | 各机型结算机台数统计 |
| 6 | 机型明细 | 3,000+ | 厂房、机台基础信息台账 |
| 7 | 开机数量 | 470+ | 各机型开机数量统计 |
| 8 | 物料表 | 470+ | 物料基础信息（供下拉匹配） |
| 9 | 156项 | - | 基础物料156项，为其他模块提供自动回填数据源 |

> 💡 侧边栏菜单使用简称：原始记录 →「维修记录」，送货超比统计 →「超比统计」。

---

## 2. 快速入门

### 2.1 访问系统

在浏览器中打开系统地址（由管理员提供），出现登录页面。

### 2.2 登录

系统使用**手机号 + 短信验证码**登录：

1. 输入手机号（账号由管理员创建）
2. 点击 **"获取验证码"**，手机收到6位验证码
3. 输入验证码，点击 **"登录"**

> ⚠️ 系统不开放公开注册。新用户需要由**管理员**在「用户管理」页面中创建。详见 [15.6 注册新用户](#156-注册新用户)。

### 2.3 账号说明

- 登录账号为**手机号**，由管理员在「用户管理」中创建，系统没有统一的默认账号
- 登录不设密码，通过短信验证码验证身份

### 2.4 全局语言设置

系统所有日期选择器均已配置为**中文**显示：
- 月份：一月 ～ 十二月
- 星期：星期一 ～ 星期日
- 日期格式：yyyy-MM-dd / yyyy-MM-dd HH:mm

> 无需额外设置，登录后自动生效。

### 2.5 界面布局

```
┌─────────────────────────────────────────────┐
│  Header: 系统名称 │ 公司选择 │ 🔔日志 │ 用户头像 │
├────────┬────────────────────────────────────┤
│ 侧边栏  │                                    │
│        │         内容区域                     │
│ 导航菜单 │      (表格/表单/操作)              │
│        │                                    │
└────────┴────────────────────────────────────┘
```

- **顶部栏**: 公司选择下拉框、操作日志快捷入口(管理员)、用户菜单
- **侧边栏**: 各模块导航菜单（可折叠）
- **内容区**: 数据表格、搜索筛选、操作按钮

### 2.6 公司切换

顶部下拉框可以选择公司，切换后所有页面自动加载对应公司的数据。不选择则显示所有公司数据。

---

## 3. 权限体系

### 3.1 角色说明

| 角色 | 权限范围 |
|------|----------|
| **管理员 (admin)** | 全部页面可见、可操作所有数据、可管理用户、可注册新用户 |
| **普通用户 (user)** | 除管理页面外全部可见、仅能修改自己创建的数据 |

### 3.2 权限矩阵

| 功能 | 管理员 | 普通用户 |
|------|:--:|:--:|
| 查看所有业务数据 | ✅ | ✅ |
| 新增数据 | ✅ | ✅ |
| 编辑/删除**自己**创建的数据 | ✅ | ✅ |
| 编辑/删除**他人**创建的数据 | ✅ | ❌ |
| Excel 导入/导出 | ✅ | ✅ |
| 模板下载 | ✅ | ✅ |
| 公司管理 | ✅ | ❌ |
| 操作日志 | ✅ | ❌ |
| 用户管理（角色/密码/删除） | ✅ | ❌ |
| 注册新用户 | ✅ | ❌ |

### 3.3 数据归属

- 所有新增的数据都会自动记录**创建人**（当前登录用户的真实姓名）
- 每张表格都有"创建人"列，可以识别数据来源
- 修改数据会更新"最后修改人"
- 所有操作都会记录在操作日志中

---

## 4. 送货记录

**用途**: 记录物料送货全量基础信息，是核心录入表单。

### 4.1 表格字段

| 字段 | 类型 | 说明 |
|------|------|------|
| ID | 数字 | 系统自动生成 |
| 日期 | 日期 | 默认当天，可修改 |
| 年月 | 文本 | 自动计算，格式 FY2607 |
| 类别 | 文本 | 如"风扇类" |
| 物料名称 | 文本 | |
| 规格型号 | 文本 | |
| 物料编码 | 文本 | BYD物料编码 |
| 物料序列号 | 文本 | |
| 数量 | 数字 | |
| 单位 | 文本 | |
| 品牌 | 文本 | |
| 产品属性 | 下拉 | 新品 / 维修品 / 免费 |
| 厂房 | 文本 | |
| 送货单号 | 文本 | |
| 备注 | 文本 | 自由填写的备注信息 |
| 创建人 | 文本 | 自动记录 |
| 更新时间 | 时间 | 自动记录 |

### 4.2 功能操作

| 操作 | 说明 |
|------|------|
| **搜索** | 支持按类别、产品属性(含"免费")、厂房精确筛选，按日期范围筛选，关键词模糊搜索（物料编码/名称/规格/序列号/品牌） |
| **新增** | 点击"新增"按钮，填写表单后提交 |
| **编辑** | 点击操作列的"编辑"按钮 |
| **复制** | 点击"复制"按钮，数据回填到新增表单，修改后提交为新记录 |
| **删除** | 单条删除或批量删除（勾选后点击"批量删除"） |
| **排序** | 点击列标题可排序（默认按ID降序） |
| **导入** | 支持Excel批量导入（详见第16章） |
| **导出** | 按当前筛选条件导出Excel |
| **模板** | 下载导入模板（含示例数据） |

### 4.3 物料编码自动匹配

新增/编辑对话框中，**物料编码**输入框支持智能自动匹配：

1. 在物料编码框中输入任意关键字（如"风扇"、"BYD"）
2. 系统自动从**物料表**中模糊搜索匹配的物料编码
3. 下拉列表显示 `物料编码 - 物料名称`
4. 选中后，系统**自动回填**以下字段：
   - **类别** — 从物料表带入
   - **物料名称** — 从物料表带入
   - **规格型号** — 从物料表带入

> 💡 这大大减少了手动输入的工作量，同时避免手动填写不一致的问题。

### 4.4 OCR 识别填充

新增数据时支持OCR图片识别自动填充（详见第17章）。

---

## 5. 原始记录

**用途**: 金属厂物料上机、下机原始台账记录，关联送货数据。

### 5.1 表格字段

| 字段 | 类型 | 说明 |
|------|------|------|
| ID | 数字 | 自动生成 |
| 日期 | 日期 | |
| 年月 | 文本 | 自动生成 FY2607 |
| 班次 | 下拉 | 白班 / 夜班 |
| 厂房 | 文本 | |
| 序号 | 文本 | |
| 机号 | 文本 | |
| 诊断人 | 文本 | |
| 维修人 | 文本 | |
| 报修时间 | 时间 | 格式 HH:mm，精确到分钟 |
| 开始时间 | 时间 | 格式 HH:mm，精确到分钟 |
| 结束时间 | 时间 | 格式 HH:mm，精确到分钟 |
| 维修工时(h) | 数字 | **自动计算**: 结束-开始 |
| 停机工时(h) | 数字 | **自动计算**: 结束-报修 |
| 机型 | 文本 | |
| 故障现象 | 文本 | |
| 维修描述 | 文本 | 对应维修过程的描述 |
| 物料编码 | 文本 | 支持自动匹配（从156项表模糊搜索） |
| 配件名称 | 文本 | |
| 数量 | 数字 | |
| 上机物料号 | 文本 | |
| 下机物料号 | 文本 | 输入后自动查询质保信息 |
| 上次上机时间 | 日期 | **实时查询**：输入下机物料号时自动查询 |
| 是否过保 | 文本 | **实时计算**: 未过保/已过保/无 |
| 备注 | 文本 | |
| 确认人 | 文本 | |
| 送货记录引用 | 文本 | |
| 创建人 | 文本 | 自动记录 |

### 5.2 表单新增/编辑布局

对话框中字段按以下分组排列：

```
Row 1:  日期 | 班次 | 厂房
Row 2:  序列号 | 机台号 | 机型
Row 3:  诊断人 | 维修人 | 确认人
Row 4:  报修时间 | 开始时间 | 结束时间  ← HH:mm 格式
Row 5:  故障现象 | 维修描述
Row 6:  料号(自动匹配156项) | 配件名称(自动回填)
Row 7:  数量 | 上机物料号 | 下机物料号
Row 8:  上次上机时间 | 是否过保  ← 实时查询显示(只读)
Row 9:  送货记录引用 | 备注
```

### 5.3 料号自动匹配156项

在新增或编辑对话框中，**料号**输入框支持从"156项"表智能自动匹配：

1. 在料号框中输入任意关键字
2. 系统自动从**156项**表中模糊搜索匹配的料号
3. 下拉列表显示 `料号 - 配件名称/系统名称`
4. 选中后，系统**自动回填**：
   - **配件名称** — 从156项表带入

> 💡 相比之前的物料表匹配，156项表提供了更精准的配件名称数据。如未查到匹配数据，配件名称保持空值。

### 5.4 过保质保实时查询

在新增或编辑对话框中，当输入**下机物料号**后：

1. 系统等待输入停顿 0.5 秒后自动查询
2. 查询本表中该物料号最近一次作为**上机物料号**出现的日期
3. 自动显示：
   - **上次上机时间** — 查询结果或无记录显示 `-`
   - **是否过保** — 距上次上机 < 6个月 → `未过保`；≥ 6个月 → `已过保`；无记录 → `无`

> 💡 这是一个纯查询展示功能，不会修改数据。过保状态在提交数据时由后端最终计算并存入数据库。

### 5.5 自动计算逻辑

| 字段 | 计算方式 |
|------|----------|
| **年月** | 从日期提取 → `FYyyMM`（如2026年7月 → FY2607） |
| **维修工时** | `结束时间 - 开始时间`，转换为小时 |
| **停机工时** | `结束时间 - 报修时间`，转换为小时 |
| **上次上机时间** | 查询当前下机物料号在本表"上机物料号"列中最后出现的日期 |
| **是否过保** | 上次上机距今 < 6个月 → "未过保"；≥ 6个月 → "已过保"；无记录 → "无" |

### 5.6 筛选条件

额外支持按班次、是否过保筛选。

> ⚠️ 自动计算字段由后端自动处理，前端无需填写，填写了也会被覆盖。

### 5.7 送货记录引用自动回填

输入**上机物料号**失焦后，系统自动查询本月送货记录中匹配的记录数并填入"送货记录引用"字段；Excel 导入时同样会自动计算。

> 💡 引用数量 = 送货记录表中当月同序列号（或物料名称）的记录数，供参考，可手动修改。

---

## 6. 未过保物料

**用途**: 维护与维修记录关联的未过保物料台账。基础字段可从"选择维修记录"一键回填，派生字段（唯一标识编号、第几次、超六个月、使用时长等）由系统自动计算。

### 6.1 表格字段

| 字段 | 类型 | 说明 |
|------|------|------|
| 日期 | 日期 | |
| 厂房 | 文本 | |
| 机台号 | 文本 | |
| 厂房+机台号 | 文本 | **自动计算**: 厂房-机台号 |
| 物料编码 | 文本 | |
| 类别 | 文本 | 按物料编码从物料表自动回填 |
| 配件名称 | 文本 | |
| 数量 | 数字 | |
| 未过保 | 文本 | 未过保 / 已过保，关联维修记录时自动带入 |
| 第几次 | 数字 | **自动计算** |
| 总次数 | 数字 | **自动计算** |
| 上次日期 | 日期 | **自动计算** |
| 本次日期 | 日期 | **自动计算** |
| 超六个月 | 文本 | **自动计算**: Y / N / 1st |
| 使用时长/月 | 文本 | **自动计算** |
| 上次维修人 | 文本 | **自动计算** |
| 维修金额（合约） | 数字 | |
| 唯一标识编号 | 文本 | **自动计算**: 厂房-机台物料编码 |
| 年+月 | 文本 | **自动计算**: FYyyMM |
| 设备维修调试 | 文本 | |
| 维修物料装上 | 文本 | |
| 维修人 | 文本 | |
| 上次日期+编号 | 文本 | **自动计算** |
| 本次日期+编号 | 文本 | **自动计算** |
| 创建人 | 文本 | 自动记录 |

### 6.2 选择维修记录自动回填

1. 新增/编辑时点击 **"选择维修记录"** 按钮
2. 弹出对话框展示维修记录列表，支持搜索（机台号/料号/维修人/厂房/维修描述）、按是否过保和日期范围筛选
3. 已被关联的维修记录不再展示（一个维修记录只允许关联一条未过保物料）
4. 点击某一行，**自动回填**基础字段：日期、厂房、机台号、设备维修调试、维修物料装上、维修人、未过保、配件名称、数量、物料编码
5. 回填后自动重新计算全部派生字段

### 6.3 派生字段自动计算

| 字段 | 计算方式 |
|------|----------|
| **唯一标识编号** | 厂房-机台物料编码（机台号与物料编码之间无分隔符，如 B5-H1115300812-00） |
| **厂房+机台号** | 厂房-机台号 |
| **年+月** | 从日期提取 → FYyyMM |
| **第几次 / 总次数** | 同一唯一标识编号在表内出现的次数 |
| **本次日期 / 上次日期** | 本次为当前日期，上次为该编号上一次出现的日期 |
| **本次/上次日期+编号** | Excel 序列号 + 唯一标识编号 |
| **超六个月** | 距上次日期 ≥ 6 个月 → Y，否则 N；无上次记录 → 1st |
| **使用时长/月** | 距上次日期的整月数（零头天数舍弃） |
| **上次维修人** | 该编号上一次记录的维修人 |

> ⚠️ 派生字段由后端在新增/编辑/导入时统一重算，覆盖前端传入值。表单中"重新计算"按钮可手动刷新预览。

### 6.4 功能操作

| 操作 | 说明 |
|------|------|
| **搜索** | 关键词（机台号/料号/配件/维修人/唯一标识/厂房+机台号…）+ 厂房 + 未过保 + 日期范围筛选 |
| **新增** | 点击"新增"，可先"选择维修记录"回填，也可手动填写 |
| **编辑** | 点击操作列的"编辑" |
| **复制** | 复制为新记录，复制时清空维修记录关联 |
| **删除** | 单条删除或批量删除 |
| **排序** | 点击列标题排序（默认按ID降序） |
| **导入** | Excel 批量导入；未过保字段为空时按 厂房+机台号+料号+日期 自动反查维修记录回填 |
| **导出** | 按当前筛选条件导出 |
| **模板** | 下载导入模板（含示例数据） |

---

## 7. 送货超比统计

**用途**: 按月统计物料送货、上机用量、超比数据，支持每日明细记录。

### 7.1 表格字段

| 字段 | 类型 | 说明 |
|------|------|------|
| ID | 数字 | |
| 类别 | 文本 | |
| 料号 | 文本 | |
| 系统名称 | 文本 | |
| 配件名称 | 文本 | |
| 单台机用量 | 数字 | 每台机器的标准用量 |
| 比例(%) | 数字 | 百分比数值，如输入 5 表示 5% |
| 含税单价 | 数字 | |
| 机台数 | 数字 | |
| 送货数量 | 数字 | 自动查询，只读 |
| 上机数量 | 数字 | 自动查询，只读 |
| 当月返修 | 数字 | 自动查询，只读 |
| 约定比例数量 | 数字 | 自动计算 |
| 超比数量合计 | 数字 | 自动计算 |
| 超比含税金额合计 | 数字 | 自动计算 |
| 统计日期 | 日期 | |
| 年月 | 文本 | |
| 创建人 | 文本 | 自动记录 |

### 7.2 智能自动回填

新增/编辑超比统计时，系统支持从"156项"表自动匹配数据并自动查询统计字段：

#### 料号自动匹配

1. 在**料号**输入框中输入关键字
2. 系统自动从**156项**表模糊搜索匹配的料号
3. 下拉列表显示 `料号 - 配件名称/系统名称`
4. 选中后，系统**自动回填**以下字段（来自156项表）：
   - 类别、系统名称、配件名称、单台机用量、比例、含税单价
5. 同时**自动查询并回填**（来自其他业务表）：
   - **机台数** — 从结算机台数表按料号+月份汇总
   - **送货数量** — 送货记录表中当月同料号的记录数
   - **上机数量** — 原始记录表中当月同料号的记录数
   - **当月返修** — 原始记录表中当月同料号且"未过保"的记录数
   - **每日明细** — 当月每一天的送货数量统计

#### 系统名称自动匹配

1. 在**系统名称**输入框中输入文字
2. 系统自动从**156项**表模糊搜索匹配的系统名称
3. 选中后自动回填对应数据（同上）

> 💡 自动查询的字段（送货数量、上机数量、当月返修、每日明细）均为**只读**，不可手动修改。如需修正数据，请在源表中调整后等待定时刷新或重新打开表单。
>
> ⚠️ 通用规则：如自动查询未匹配到数据，对应字段保持空值，不做回填。

### 7.3 自动计算公式

| 字段 | 计算公式 | 触发时机 |
|------|----------|----------|
| **约定比例数量** | `机台数(用户输入) × 单台机用量 × 比例` | 提交时后端计算 |
| **超比数量合计** | `送货数量 − 当月返修` | 提交时后端计算 |
| **超比含税金额合计** | `(含税单价 × 超比数量) ÷ 1.13` | 提交时后端计算 |

> ⚠️ 注意：超比含税金额已自动除以1.13进行去税处理。

### 7.4 汇总行（全部数据）

表格上方显示**全表汇总数据**，按以下公式计算（单位：万元）：

| 汇总项 | 计算公式 |
|--------|----------|
| 送货金额合计 | Σ(含税单价 × 送货数量) ÷ 1.13 ÷ 10000 |
| 上机金额合计 | Σ(含税单价 × 上机数量) ÷ 1.13 ÷ 10000 |
| 返修金额合计 | Σ(含税单价 × 当月返修) ÷ 1.13 ÷ 10000 |
| 比例内金额合计 | Σ(含税单价 × 约定比例数量) ÷ 1.13 ÷ 10000 |
| 超比金额合计 | Σ(含税单价 × 超比数量) ÷ 1.13 ÷ 10000 |
| 超比含税总额 | Σ(超比含税金额) ÷ 10000 |

> 📐 公式说明：先将每行的含税单价乘以对应数量，求和后除以 1.13（去税），再除以 10000（换算为万元）。汇总行读取当前**全部数据**（不受分页限制，受筛选条件影响）。

### 7.5 每日明细

每条统计记录自动展示当月1日至月末最后一天的送货数量明细（根据所选统计日期动态确定天数）。

- 数据来源：自动从**送货记录**表统计当月每一天的送货次数
- 只读展示，不可手动修改
- 新增/编辑时随料号选中自动填充
- 有值的日期显示为红色，便于快速定位

---

## 8. 结算机台数

**用途**: 各机型物料用量、占比、结算机台数据统计。

### 8.1 表格字段

| 字段 | 说明 |
|------|------|
| 料号 | |
| 类别 | |
| 配件名称 | |
| 单台用量 | |
| 比例 | |
| 含税单价 | |
| 质保期 | 默认值为"6个月" |
| 价格类型 | 下拉框选择：新品价 / 维修价，默认为空 |
| 统计月份 | 格式 yyyy-MM |
| 机型 | |
| 结算机台数 | |
| 备注 | |
| 创建人 | 自动记录 |

### 8.2 料号自动匹配156项

新增/编辑时，**料号**输入框支持从"156项"表智能自动匹配：

1. 在料号框中输入任意关键字
2. 系统自动从**156项**表中模糊搜索匹配的料号
3. 下拉列表显示 `料号 - 配件名称/系统名称`
4. 选中后，系统**自动回填**以下字段（来自156项表）：
   - 类别、配件名称、单台用量、比例、含税单价

> ⚠️ 通用规则：如未查到匹配数据，对应字段保持空值。

### 8.3 开机数量选择弹窗

在新增/编辑对话框中，提供了一个便捷的**"选择开机数量"**按钮：

1. 先选择**统计月份**（如 2026-07）
2. 点击 **"选择开机数量"** 按钮
3. 弹出对话框，展示该月份"开机数量"表中的数据：
   - **机型** — 机台型号
   - **开机台数** — 该机型的开机数量
4. 点击某一行数据，**自动填充**：
   - **机型** → 选中行的机型
   - **结算机台数** → 选中行的开机台数

> 💡 这大大简化了录入流程，避免了手动查找和输入的错误。

### 8.4 字段默认值

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 质保期 | `6个月` | 新增时自动填充，可手动修改 |
| 价格类型 | 空 | 下拉框选择：新品价 / 维修价 |

### 8.5 筛选条件

| 筛选条件 | 说明 |
|----------|------|
| 关键词 | 模糊搜索料号/配件名称/类别/机型 |
| 机型 | 精确筛选机型 |
| 统计月份 | 按月筛选 |

---

## 9. 机型明细

**用途**: 归档厂房、机台基础信息，为上机记录、维修统计提供基础台账。

| 字段 | 说明 |
|------|------|
| 厂房 | |
| 机台号 | |
| 机台品牌 | |
| 创建人 | 自动记录 |

筛选支持：厂房、品牌精确筛选 + 关键词模糊搜索。

---

## 10. 开机数量

**用途**: 各机型开机数量及占比统计，支持基准线自动计算。

### 10.1 表格字段

| 字段 | 说明 |
|------|------|
| 机型 | |
| 数量 | 该机型的开机台数 |
| 占比(%) | **自动计算**（非基准线记录自动根据基准线计算） |
| 统计月份 | 如 2026-07 |
| 备注 | |
| 创建人 | 自动记录 |

### 10.2 基准线机制

每月有且只有一条**基准线记录**，代表"当月开机总数"，占比固定为 **100%**。

#### 设为基准线

1. 新增或编辑记录时，勾选 **「设为基准线（当月开机总数）」** 复选框
2. 勾选后占比自动设为 100%（不可手动修改）
3. 提交后，系统自动将同月原有的基准线降级，并重新计算同月所有记录的占比

#### 自动重算规则

- **基准线记录**：占比 = 100%
- **其他记录**：占比 = `(该记录数量 ÷ 基准线数量) × 100`
- 基准线的数量变更时，同月所有非基准线记录**自动重算**

#### 删除保护

- 当月存在其他记录时，基准线**不可单独删除**
- 需先将其他记录设为基准线，或删除该月所有记录
- 若强制删除基准线（同月无其他记录时），系统会提示

#### 示例

```
2026-07 月数据：
  机型A: 500台 [✓ 基准线] → 占比 100%
  机型B: 300台            → 占比 (300/500)×100 = 60%
  机型C: 200台            → 占比 (200/500)×100 = 40%
```

> 💡 如果修改机型A的数量为 600，机型B和C的占比会自动更新为 50% 和 33.33%。

---

## 11. 物料表

**用途**: 物料基础信息维护。

> 📌 **注意**：自 v2.2 起，原始记录、超比统计、结算机台数的料号自动匹配功能已切换为从**156项**表查询，物料表的快速搜索接口仍保留可用。

### 11.1 搜索功能

- **分页列表**: 支持关键词模糊搜索
- **快速搜索**: `/api/material/search` 接口返回不分页结果（最多15条）

---

## 12. 156项

**用途**: 基础物料156项数据维护，作为其他模块（原始记录、超比统计、结算机台数）的自动回填数据源。

### 12.1 表格字段

| 字段 | 类型 | 说明 |
|------|------|------|
| ID | 数字 | 系统自动生成 |
| 类别 | 文本 | 如"风扇类" |
| 料号 | 文本 | **唯一**，必填 |
| 系统名称 | 文本 | |
| 配件名称 | 文本 | |
| 单台机用量 | 数字 | |
| 比例 | 数字 | 表格显示为百分比，自动去除尾部多余的0 |
| 含税单价 | 数字 | |
| 创建人 | 文本 | 自动记录 |

### 12.2 比例显示

表格中比例列的显示规则：
- 后端存储为小数（如 0.0012 表示 0.12%）
- 前端自动乘以100转换为百分比
- **自动去除尾部多余的0**：`0.1000%` → `0.1%`，`1.0000%` → `1%`，`0.1234%` 保持不变

### 12.3 功能操作

| 操作 | 说明 |
|------|------|
| **搜索** | 支持按料号/系统名称/配件名称/类别模糊搜索 |
| **新增** | 点击"新增"填写表单，料号必填且唯一 |
| **编辑** | 点击操作列的"编辑"按钮 |
| **复制** | 点击"复制"按钮，数据回填到新增表单，修改料号后提交为新记录 |
| **删除** | 单条删除或批量删除 |
| **排序** | 点击列标题可排序 |
| **导入** | 支持Excel批量导入，自动校验料号唯一性 |
| **导出** | 按当前筛选条件导出Excel |
| **模板** | 下载导入模板（仅含表头，无示例数据） |

### 12.4 料号唯一性校验

- **新增时**：系统自动检查料号是否已存在，重复时提示"料号已存在"
- **编辑时**：修改料号同样会校验唯一性（排除自身）
- **导入时**：逐行校验，重复料号会出现在失败明细中

### 12.5 Excel 导入模板

> ⚠️ 模板**只有表头，不含示例数据**。请按照表头格式填写数据后导入。

导入失败时会弹窗显示详细的失败原因，包括失败行号和具体错误描述。

### 12.6 在系统中的角色

156项表是整个系统自动回填机制的**核心数据源**：

```
156项 (base_material_156)
    │
    ├──→ 原始记录: 料号模糊查询 → 回填配件名称
    │
    ├──→ 超比统计: 料号/系统名称模糊查询 → 回填所有基础字段
    │
    └──→ 结算机台数: 料号模糊查询 → 回填基础字段
```

> 💡 156项表的数据质量直接影响其他模块的自动回填效果。建议优先将基础物料数据录入完整。

---

## 13. 公司管理 🔐

**仅管理员可访问。**

管理公司信息，目前系统中默认有"金属厂"。

| 操作 | 说明 |
|------|------|
| 查看列表 | 所有用户均可查看公司列表（用于顶部下拉切换） |
| 新增公司 | 仅管理员 |
| 编辑公司 | 仅管理员 |
| 删除公司 | 仅管理员 |

---

## 14. 操作日志 🔐

**仅管理员可访问。**

记录系统中所有数据变更操作（INSERT/UPDATE/DELETE），用于追溯和审计。

| 字段 | 说明 |
|------|------|
| 操作人 | 执行操作的用户名 |
| 操作类型 | INSERT / UPDATE / DELETE |
| 表名 | 被操作的数据表 |
| 记录ID | 被操作记录的主键 |
| 详情 | 变更内容（JSON格式，DELETE时为空） |
| IP地址 | 操作来源IP |
| 操作时间 | |

### 快捷入口

管理员点击右上角🕐图标可以快速查看最近20条操作日志。

### 筛选功能

支持按操作人、表名、操作类型、日期范围筛选。

---

## 15. 用户管理 🔐

**仅管理员可访问。**

### 15.1 用户列表

显示所有注册用户：用户名、真实姓名、角色、注册时间。

### 15.2 角色管理

| 操作 | 按钮 | 说明 |
|------|------|------|
| 提升为管理员 | "提升为管理员" | 将普通用户升级为管理员 |
| 降级为普通用户 | "降级为普通用户" | 将管理员降级（不能降级自己） |

### 15.3 重置密码

1. 点击用户行的 **"重置密码"** 按钮
2. 输入新密码（至少6位）
3. 点击 **"确认重置"**

### 15.4 删除用户

1. 点击用户行的 **"删除"** 按钮
2. 确认删除

> ⚠️ 安全限制: 不能删除自己、不能删除最后一个管理员。

### 15.5 删除用户

1. 点击用户行的 **"删除"** 按钮
2. 确认删除

> ⚠️ 安全限制: 不能删除自己。

### 15.6 注册新用户 🆕

管理员在用户管理页面可以创建新用户：

1. 点击页面顶部的 **"注册新用户"** 按钮
2. 在弹出的对话框中填写：
   - **用户名** — 登录账号
   - **真实姓名** — 显示在"创建人"字段中
   - **密码** — 至少6位
   - **确认密码** — 再次输入密码确认
3. 点击 **"确认注册"**
4. 新用户默认为**普通用户**角色，管理员可在用户列表中提升为管理员

> ⚠️ 系统不开放公开注册页面。只有管理员才能在「用户管理」中注册新用户。

---

## 16. Excel导入导出

### 16.1 导入模板下载

每个模块页面都有 **"模板"** 按钮，下载Excel导入模板。

> 📌 156项模板**仅含表头**，无示例数据。其他模块模板含一行示例数据，导入前请删除或替换示例行。

### 16.2 导入数据

1. 点击 **"导入"** 按钮
2. 选择按模板填好的 .xlsx 文件
3. 系统自动校验并导入
4. 导入完成后显示结果：总条数、成功数、失败数
5. 如有失败，会**弹窗显示详细失败原因**（包括行号和具体错误描述）

### 16.3 导入常见失败原因

| 原因 | 说明 |
|------|------|
| 料号/物料编码为空 | 必填字段未填写 |
| 料号已存在 | 156项表中该料号已有记录 |
| 数据解析失败 | Excel单元格格式不匹配（如文本写入数字列） |
| 批量写入失败 | 数据库错误 |

### 16.4 导出数据

点击 **"导出"** 按钮，按照当前筛选条件导出全部匹配数据为Excel文件。

> 导出会忽略分页限制，导出当前筛选条件下的**全部**数据。

---

## 17. OCR图片识别

**适用模块**: 送货记录新增页面

### 17.1 使用方法

1. 在送货记录新增对话框中，点击OCR上传按钮
2. 上传包含物料信息的图片
3. 系统自动识别图片中的文本
4. 识别结果自动填充到对应表单字段
5. 人工审核并修正识别误差
6. 确认无误后提交数据

### 17.2 注意事项

- 图片清晰度直接影响识别准确率
- 识别结果需要人工审核确认
- 识别失败或图片模糊时会有明确提示，可重新上传或手动填写

> 注：OCR功能当前处于开发阶段，具体使用方法以后续更新为准。

---

## 18. 定时任务

### 18.1 超比统计数据自动刷新

系统内置了定时任务，**每天凌晨3点**自动刷新所有超比统计数据。

#### 刷新范围

- 刷新**所有月份**的超比统计记录

#### 刷新内容

| 刷新项 | 数据来源 |
|--------|----------|
| 送货数量 | 重新统计送货记录表中当月同料号记录数 |
| 上机数量 | 重新统计原始记录表中当月同料号记录数 |
| 当月返修 | 重新统计原始记录表中当月同料号且"未过保"记录数 |
| 每日明细 | 重新统计每天送货数量 |
| 约定比例数量 | 重新计算：机台数 × 单台机用量 × 比例 |
| 超比数量合计 | 重新计算：送货数量 − 当月返修 |
| 超比含税金额合计 | 重新计算：(含税单价 × 超比数量) ÷ 1.13 |

#### 设计原理

工厂环境下数据持续录入，每日凌晨刷新能保证统计数据的时效性。

> 💡 如果在两次定时刷新之间需要立即看到最新统计结果，可以重新打开超比统计的新增/编辑对话框，选中料号后会实时查询最新数据。
>
> 📌 刷新时间可通过数据库配置项 `scheduler.cron` 调整，默认每天凌晨 3 点。

---

## 19. 常见问题

### Q1: 忘记密码怎么办？
联系管理员，管理员在"用户管理"中可以重置密码。

### Q2: 为什么我不能编辑某些数据？
普通用户只能编辑/删除**自己创建**的数据。查看"创建人"列确认数据归属。如需修改他人数据，请联系管理员。

### Q3: 为什么侧边栏有些菜单看不到？
"公司管理"、"操作日志"、"用户管理"仅对管理员可见。如果你应该有管理员权限，请联系现有管理员提升你的角色。

### Q4: 导入Excel失败怎么办？
首先查看导入结果弹窗中的**失败详情**，确认具体失败原因。常见原因：
- 是否使用了正确的模板格式
- 必填字段是否填写完整（如料号不能为空）
- 数字列是否填写了非数字内容
- 料号是否已在156项表中存在
- 日期格式是否正确（yyyy-MM-dd）

### Q5: 数据量大时页面慢怎么办？
- 使用筛选条件缩小查询范围
- 调整每页显示条数
- 导出Excel后离线分析

### Q6: 如何查看谁修改了数据？
管理员在"操作日志"中可以查看所有操作记录，包括操作人、时间、变更内容。

### Q7: 自动计算的字段为什么不显示？
维修工时、停机工时、是否过保等字段由后端自动计算。只有在新增或修改数据时才会触发计算，且必须提供必要的源字段（如开始时间、结束时间）。

### Q8: 超比统计中输入料号后没有自动回填数据怎么办？
检查以下几点：
- "156项"表中是否存在该料号的记录（可在156项页面搜索确认）
- 统计日期是否已选择（自动查询依赖日期确定月份）
- 送货记录/原始记录表中当月是否有对应数据（无数据时对应字段为0或空）

### Q9: 156项表料号重复怎么办？
系统强制料号唯一：
- 新增/编辑时重复 → 提示"料号已存在"
- Excel导入时重复 → 该行导入失败，弹窗显示失败明细
- 请先在156项表中检查是否已有该料号

### Q10: 超比统计的每日明细显示为0或不全？
每日明细是从送货记录表自动统计的。如果某天没有送货记录，该天数量就是0。数据每天凌晨3点自动刷新一次，如需立即更新请重新打开表单。

### Q11: 结算机台数中点击"选择开机数量"没有数据显示？
请先选择**统计月份**，系统会根据月份查询"开机数量"表中的数据。如果该月份没有记录，弹窗表格为空。

### Q12: 如何创建新用户账号？ 🆕
系统不开放公开注册。请管理员登录后，在「用户管理」页面点击**"注册新用户"**按钮创建。

---

## 附录A: 排序与分页

### 排序
- 所有列表页支持点击列标题排序
- 默认按 ID 降序排列（最新数据在前）
- 排序字段经过安全校验，无法注入

### 分页
- 默认每页显示 20 条
- 可切换为 20/50/100 条
- 翻页和切换每页条数后保持当前筛选条件

---

## 附录B: 浏览器兼容性

推荐使用以下浏览器的较新版本：
- Chrome / Edge（推荐）
- Firefox
- Safari

---

## 附录C: 数据安全

- 所有API请求通过JWT Token认证，24小时有效
- 密码使用BCrypt加密存储
- 操作日志完整记录所有数据变更
- 普通用户无法操作他人创建的数据
- 管理员的所有操作同样被记录
- 注册新用户仅限管理员操作，不对外开放

---

## 数据看板

菜单「数据看板」提供 5 张按月统计的看板，数据实时从业务表（未过保物料、维修记录）聚合，**不需要手工维护**：

| 看板 | 统计维度 | 说明 |
|------|---------|------|
| 维修金额 | 机台 × 月份 | 未过保物料的维修金额按月 × 厂房+机台号汇总 |
| 故障频次 | 机台 × 月份 | 维修记录条数按月 × 厂房+机台号统计 |
| 物料频次 | 料号 × 月份 | 未过保物料条数按月 × 物料编码统计（物料编码可空，空的不计入） |
| 返修频次 | 料号 × 月份 | 非首次维修（有上次日期）的记录按月 × 料号统计，近似口径 |
| 物料返修率 | 料号 × 月份 | 返修频次 ÷ 物料频次，百分比显示 |

使用要点：
- **年份选择**：默认当年，可切换前一年/后一年（如 2026/2027），每年自动显示 FYxx01~FYxx12 共 12 个月列。录入新年度数据后无需任何配置，看板自动出现新年度。
- **公司切换**：与全系统一致，顶部公司下拉切换后看板按当前公司过滤。
- **合计行**：维修金额/故障频次/物料频次/返修频次 4 张看板均带合计行——每个月份列 = 当月全部行（机台或料号）的合计，小计/合计列 = 全年总计，金额列 = 全部行金额合计（与原表表头上方汇总行一致）。页面中合计行固定在表格顶部（每页可见），导出的 Excel 中位于表头上方第 1 行。
- **导出**：右上角「导出 Excel」，按当前看板与年份导出（含月份列与合计列）。
- 机台类看板行数较多时分页展示（每页 50 台）。

> ⚠️ 口径说明：故障频次依赖维修记录数据，若历史维修记录未全量导入，机台清单与数值会少于 Excel 原表；返修频次以「非首次维修」近似原表返修标记（覆盖率约 96.7%）。

---

> 📞 技术支持: 联系系统管理员
> 📅 文档版本: v2.4 | 2026-08-03
