# 金属厂数据管理系统 · 用户使用手册

> 本手册写给日常使用系统的同事，不涉及任何技术细节。
> 阅读重点：**凡是标「✍️ 手动」的地方需要你录入/操作；标「🤖 自动」的地方系统自己算，不用你管。**

---

## 一、这个系统是干什么的

以前工厂的数据都存在大 Excel 表格里，多人共用一份文件，又卡又容易乱。这个系统把数据搬到了网页上，大家各自登录各自的账号，数据统一存放、自动统计。

系统里一共有 **10 个功能页面**，按作用分成三类：

| 类别 | 页面 | 作用 |
|------|------|------|
| 基础资料 | 156项、物料表、机型明细 | 相当于"字典"，先录好，后面录数据时自动带出信息 |
| 业务数据 | 送货记录、维修记录、未过保物料 | 每天要录的核心数据 |
| 统计报表 | 超比统计、结算机台数、开机数量、数据看板 | 大部分数字系统自动算，你只管核对 |

> 提示：菜单里的「原始记录」就是大家常说的**维修记录**；「送货超比统计」简称**超比统计**。

---

## 二、登录

1. 浏览器打开系统网址（找管理员要）。
2. 输入你的**手机号**，点「获取验证码」，手机收到 6 位数字。
3. 输入验证码，点「登录」。

账号由管理员统一创建，系统不开放自己注册。登录后顶部可以切换**公司**（如金属厂），切换后所有页面自动只显示该公司的数据。

---

## 三、数据是怎么流动的（先看这张图）

```
① 基础资料（先录一次，长期用）
   156项 ──→ 维修记录自动带出"配件名称"
      │      超比统计自动带出"类别/系统名称/配件名称/用量/比例/单价"
      │      结算机台数自动带出"类别/配件名称/用量/比例/单价"
   物料表 ──→ 送货记录自动带出"类别/物料名称/规格型号"
   机型明细 → 机台基础信息（厂房/机台号/品牌）

② 业务数据（每天录）
   送货记录 ──→ 超比统计的"送货数量/每日明细"
   维修记录 ──→ 自动生成"未过保物料"；超比统计的"上机数量"
   未过保物料 → 超比统计的"当月返修"

③ 统计结果（系统自动算）
   超比统计 / 结算机台数 / 开机数量 / 数据看板
```

**一句话总结：先把 156项、物料表这些基础资料录全，后面录业务数据时系统才能自动带出、自动统计。**

---

## 四、各页面怎么用

### 4.1 156项（基础资料，最优先维护）

**用途**：整个系统自动回填的"源头字典"，录维修记录、超比统计、结算机台数时都会查它。

- ✍️ **手动**：新增/修改记录。填类别、料号（**必填且不能重复**）、系统名称、配件名称、单台机用量、比例、含税单价。支持 Excel 批量导入。
- 🤖 **自动**：创建人、创建/更新时间自动记录。
- 注意：料号重复会被系统拦下；比例显示为百分比（如 0.12%），录入时按页面提示填。

### 4.2 物料表（基础资料）

**用途**：送货记录录入时的"字典"（按物料编码匹配）。

- ✍️ **手动**：维护类别、物料名称、规格型号、物料编码。
- 🤖 **自动**：创建人自动记录。
- 注意：这个表的用途是给送货记录做自动匹配，维护得越全，录送货记录越省事。

### 4.3 机型明细（基础资料）

**用途**：记录厂房里有哪些机台（厂房、机台号、机台品牌）。

- ✍️ **手动**：新增/修改/删除，全部自己填，无自动计算。
- 支持按厂房、品牌筛选，关键词搜索。

### 4.4 送货记录（核心录入，每天用）

**用途**：记录物料送货信息，是后续所有统计的数据来源之一。

- ✍️ **手动**：日期（默认当天，可改）、类别、物料名称、规格型号、物料编码、物料序列号、数量、单位、品牌、产品属性（新品/维修品/免费）、厂房、送货单号、备注。
- 🤖 **自动**：
  - **年月**：按日期自动生成（如 2026年7月 → FY2607），不用填；
  - **信息带出**：在"物料编码"框输关键字，从物料表匹配，选中后自动填上类别、物料名称、规格型号；
  - **OCR 图片识别**：点上传按钮传一张送货单照片，系统把识别出的文字自动填进表单——**识别结果必须人工核对**，错了就改，核对无误再提交；
  - **创建人、时间**：自动记录。
- 支持 Excel 批量导入/导出、模板下载、复制成新记录。

### 4.5 维修记录（原始记录，核心录入，每天用）

**用途**：记录物料上机、下机的维修台账。

- ✍️ **手动**：日期、班次（白班/夜班）、厂房、序列号、机台号、机型、诊断人、维修人、确认人、报修时间、开始时间、结束时间（精确到分钟）、故障现象、维修描述、料号、数量、上机物料号、下机物料号、备注。
- 🤖 **自动**（保存时后端计算，你填了也会被覆盖）：
  - **年月**：按日期生成 FY2607 格式；
  - **厂房-机台**：由厂房+机台号拼出；
  - **维修工时**：结束时间 − 开始时间（单位**分钟**）；
  - **停机工时**：结束时间 − 报修时间（单位**分钟**）；
  - **跨天修正**：如果结束时间早于开始/报修时间（比如跨了零点），系统自动加一天再算，不用你改；
  - **配件名称**：料号匹配 156项 自动带出（可手动改）；
  - **送货记录引用**：填了上机物料号后，自动查当月送货记录里匹配的条数填进去（可手动改）；
  - **上次上机时间 / 是否过保**：输入**下机物料号**后自动实时查询显示——查这个料号上次上机是什么时候，距现在不到 6 个月显示"未过保"，超过 6 个月显示"已过保"，没查过显示"无"（仅展示参考，不存库）；
  - **自动生成未过保物料**：**数量 ≥ 1 时，保存维修记录会自动生成一条对应的"未过保物料"记录**；以后修改这条维修记录，未过保物料跟着同步更新；把数量改成 0 或删掉，关联的未过保物料也会被删除。
- 注意：删除维修记录前系统会提示"关联的未过保物料将一并删除"。

### 4.6 未过保物料

**用途**：未过保物料的台账，跟维修记录关联，派生字段全部自动算。

- ✍️ **手动**：两种录法任选——
  1. 点「**选择维修记录**」，从列表里挑一条，基础字段自动带出（最省事，推荐）；
  2. 全部手填：日期、厂房、机台号、维修人、上机物料、配件名称、数量、料号、处理方式等。
- 🤖 **自动**（保存时统一重算，你填了也会被覆盖）：
  - **类别**：按料号从物料表带出；
  - **配件名称**：按料号从 156项 带出（你已填过就不覆盖）；
  - **唯一标识编号**：厂房-机台号+配件名称拼成（如 `F6-A15主轴`），同一编号的物料算同一根"账"；
  - **厂房+机台号、年月**：自动拼接/生成；
  - **第几次 / 总次数**：这个唯一标识编号第几次出现 / 一共出现过几次；
  - **上次日期 / 本次日期、上次/本次日期+编号**：自动取上一次和本次的日期；
  - **超六个月**：距上次维修 ≥ 6 个月 → Y，不到 → N，第一次出现 → 1st；
  - **使用时长/月**：距上次维修的整月数；
  - **上次维修人**：上一次记录的维修人；
  - **维修金额（合约）**：**156项表里同料号的含税单价 × 数量**，自动算，手动填了也会被覆盖；
  - **未过保状态**：距上次维修不到 6 个月 → "未过保"，否则留空（表单里可以手选，但保存时以系统判定为准）。
- 提示：列表里的"总次数"是实时统计的，新增/删除后刷新页面就变。

### 4.7 超比统计（送货超比统计）

**用途**：按月统计每个料号的送货、上机、超比数量与金额，用于对账核算。

- ✍️ **手动**：
  - **料号**（或系统名称）：输入关键字从 156项 匹配，选中后自动带出一串基础信息（类别、系统名称、配件名称、单台机用量、比例、含税单价），可微调；
  - **统计日期**：选择要统计的月份；
  - 其它还能手动填的：类别、配件名称等基础字段（一般不用动）。
- 🤖 **自动**（只读，不能手改）：
  - **机台数**：从结算机台数表按料号+月份汇总；
  - **送货数量 / 送货免费**：当月送货记录里该料号的数量（"免费"单算）；
  - **上机数量**：当月维修记录里该料号的数量；
  - **当月返修**：当月未过保物料里该料号的数量；
  - **每日明细**：当月每天送货数量（1日~31日），有值的日期标红；
  - **约定比例数量** = 机台数 × 单台机用量 × 比例；
  - **超比数量合计** = 上机数量 − 当月返修 − 约定比例数量（算出来是负数按 0 算）；
  - **超比含税金额合计** = 含税单价 × 超比数量 ÷ 1.13（自动去税）；
  - **汇总行**：表格上方按万元显示当月合计（金额都除以 1.13 去税）。
- 🔄 **每天凌晨 3 点**，系统会自动把所有月份的超比统计重新刷新一遍（送货/上机/返修/每日明细都重算）。白天录完数据想立刻看最新数：重新打开该条统计的编辑框，选中料号即实时刷新。
- 支持按固定格式**语音朗读**录入（页面有提示语），进阶功能，可选。

### 4.8 结算机台数

**用途**：各机型物料用量、占比、结算机台数统计。

- ✍️ **手动**：统计月份、机型、结算机台数、价格类型（新品价/维修价）、备注。料号从 156项 匹配（自动带出类别、配件名称、单台用量、比例、含税单价，可微调）。
- 🤖 **自动**：
  - **质保期**：新增时默认填"6个月"（可改）；
  - **开机数量弹窗**：选好统计月份，点「选择开机数量」，从开机数量表挑一行，机型+结算机台数自动填上，不用手查。

### 4.9 开机数量

**用途**：各机型开机台数及占比。

- ✍️ **手动**：机型、数量、统计月份。**先录入当月机台总数，勾选「设为基准线（当月开机总数）」**——这条记录的数量就是当月的机台总数，每月有且只有一条基准线；再录入其他机型的开机数，占比系统自动算。
- 🤖 **自动**：
  - **占比**：基准线那条固定 100%；其他记录 = 该机型数量 ÷ 基准线数量 × 100%；
  - **联动重算**：改基准线的数量，或换一条基准线，当月所有占比自动重算；
  - **删除保护**：当月还有其他记录时，基准线不能单独删（要先指定新的基准线）。

### 4.10 数据看板

**用途**：5 张月度统计图表，**完全自动，不需要任何手工维护**：

| 看板 | 看什么 |
|------|--------|
| 维修金额 | 每台机每月维修花了多少钱 |
| 故障频次 | 每台机每月修了几次 |
| 物料频次 | 每个料号每月出现几次 |
| 返修频次 | 每个料号每月返修几次（非首次维修） |
| 物料返修率 | 返修频次 ÷ 物料频次 |

- 顶部选**年份**（默认当年，自动显示 FYxx01~FYxx12 共 12 个月列），录了新年度数据不用任何配置，看板自动出现；
- 右上角可导出 Excel；机台类看板行数多时分页显示（每页 50 台）。

### 4.11 Excel 批量导入导出（所有业务页面通用）

- **模板**：点「模板」下载导入模板。**注意：156项 的模板只有表头没有示例数据**，其他模块的模板带一行示例，导入前删掉示例行。
- **导入**：点「导入」选文件，导入完弹窗显示：总条数、成功数、失败数；失败会列**行号和原因**（常见：必填项为空、料号重复、日期/数字格式不对）。
- **导出**：点「导出」，按当前筛选条件导出**全部**数据（不受分页限制）。

---

## 五、日常操作建议

**第一次使用（管理员或数据负责同事）**：
1. 先把 **156项** 录全（或批量导入）——它是自动回填的源头；
2. 再维护 **物料表**、**机型明细**；
3. 用 Excel 模板把历史数据批量导入各业务表（送货记录 → 维修记录 → 未过保物料）。

**每天**：
1. 录当天的**送货记录**（可用 OCR 拍照识别，核对后提交）；
2. 录当天的**维修记录**（未过保物料会自动生成，不用单独录）；
3. 需要时到**超比统计**看当天数据（或等凌晨 3 点自动刷新）。

**每月**：
1. 核对**开机数量**：确认当月基准线是否正确；
2. 核对**结算机台数**与**超比统计**；
3. 用**数据看板**导出月度汇总。

**通用习惯**：
- 数据填错了：普通用户只能改**自己创建**的数据（看"创建人"列），改别人的要找管理员；
- 想录一条和旧记录差不多的：用**复制**按钮，改几个字段提交即可；
- 所有的新增/修改/删除都会被记录到**操作日志**（管理员可查），操作要规范。

---

## 六、常见问题

**Q1：为什么我录维修记录，没看到未过保物料自动生成？**
答：只有**数量 ≥ 1** 时才会自动生成；数量填 0 或留空不会生成。另外自动生成的记录在「未过保物料」页面里，不是维修记录页面。

**Q2：为什么我填的维修金额/未过保状态，保存后变了？**
答：这两个字段是系统按规则自动计算的（维修金额 = 156项含税单价 × 数量；未过保 = 距上次维修不到 6 个月）。想改结果，改对应的料号、数量或日期，系统会重算。

**Q3：超比统计里有些格子不能填？**
答：送货数量、上机数量、当月返修、每日明细、约定比例数量、超比数量、超比金额都是从业务数据自动统计的，**只读**。数据不对时，去源头（送货记录/维修记录/未过保物料/156项）改，然后重新打开这条统计或等凌晨 3 点自动刷新。

**Q4：为什么超比统计输入料号没有自动带出数据？**
答：按顺序检查：① 156项 里有没有这个料号；② 统计日期选没选；③ 当月送货记录/维修记录里有没有这个料号的数据。

**Q5：导入 Excel 失败怎么办？**
答：看弹窗里的失败明细（行号+原因）。常见：必填字段空着、料号重复、数字列填了文字、日期格式不是 yyyy-MM-dd。

**Q6：为什么有些菜单我看不到？**
答：公司管理、操作日志、用户管理只有**管理员**能看到。

**Q7：谁改了数据？**
答：管理员在「操作日志」里可以查到每一次新增/修改/删除的操作人、时间和变更内容。

---

> 技术支持：联系系统管理员
> 手册版本：v1.0（2026-08-05）
